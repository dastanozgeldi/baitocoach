services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: baito_coach_postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: baito_coach
    volumes:
      - baito_coach_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  baito_coach_postgres_data:
    driver: local
